numpy>=1.24,<2.1
# Pinned. Do not loosen this. GFPGAN depends on opencv-python and, left
# unpinned, installed OpenCV 5.0.0. That build makes InsightFace return zero
# faces, so detection and swap both look like they did nothing. 4.10.0.84 is
# the last known-good release. Do not install opencv-python-headless beside it.
opencv-python==4.10.0.84
onnx>=1.14.0
onnxruntime>=1.16.0
insightface==0.7.3
ffmpeg-python>=0.2.0
tqdm>=4.66.0
pillow>=10.0.0
requests>=2.31.0
PySide6>=6.6.0
