# Optional. Enables Restore faces (GFPGAN) and `--enhance`.
# These packages pull an older PyTorch stack; the desktop app runs without them.
# gfpgan/basicsr depend on opencv-python. Without the pin below, pip resolved
# that to OpenCV 5.0.0 and InsightFace stopped detecting faces. Keep 4.10.0.84.
gfpgan==1.3.8
basicsr==1.4.2
opencv-python==4.10.0.84
